import React, { useState } from "react";

import { Modal, Fade, Backdrop } from '@material-ui/core/';

// import useStyles from './styles';


function HTTestModal({ children }) {
    // const classes = useStyles();

    return (
        <div>
            INSide test modal
        </div>
    )
}

export default HTTestModal;